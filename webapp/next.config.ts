import type { NextConfig } from "next";

// Entorno de DESARROLLO de la migración (docs/06-roadmap.md).
// No añadir aquí nada que asuma que esta app ya sirve producción.
const nextConfig: NextConfig = {};

export default nextConfig;
